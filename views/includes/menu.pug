div.list-group
    if categories.length > 0
        each category in categories
            a(href=`/categories/${category.id}` class=`${category.id == selectedCategory ? "active" : ""}`).list-group-item #{category.name}
extend ../layouts/main-layout.pug

block styles
    link(rel="stylesheet" href="/css/forms.css")

block content
    form(action="/admin/add-product" method="POST")
        div.form-group.row
            label(for="name").col-sm-2.col-form-label Product Name
            div.col-sm-10
                input(type="text" name="name").form-control#name

        div.form-group.row.mt-2
            label(for="price").col-sm-2.col-form-label Price
            div.col-sm-10
                input(type="text" name="price").form-control#price

        div.form-group.row.mt-2
            label(for="imageUrl").col-sm-2.col-form-label Image
            div.col-sm-10
                input(type="text" name="imageUrl").form-control#imageUrl

        div.form-group.row.mt-2
            label(for="categoryid").col-sm-2.col-form-label Category
            div.col-sm-10
                select(name="categoryid").form-control#categoryid
                    option(value="-1") Select Category
                    each category in categories
                        option(value=`${category.id}`) #{category.name}

        div.form-group.row.mt-2
            label(for="description").col-sm-2.col-form-label Description
            div.col-sm-10
                textarea(type="text" name="description" cols="10" rows="10").form-control#description

        div.form-group.row
            div.col-sm-12
                input(type="submit" value="Save Product").btn.btn-primary.float-end.mt-2